<div class="navbar-item-container {{containerClass}}" id={{itemId}}>
  <i class="fa-regular {{iconClass}} navbar-item-icon"></i>
  {{#if showBadge}}
    <div class="navbar-item-badge">{{badgeCount}}</div>
  {{/if}}
  <span class="navbar-item-text {{spanClass}}">{{itemText}}</span>
</div>